# src/components/experiments/

Interactive ("explorable explanation") modules. React islands embedded in experiment posts.

- `ExperimentFrame` — wrapper for interactive demos (supports future sliders, toggles, viz).

Future modules (brief section 21): `LayerSlider`, `DashboardTruthMeter`,
`MachineStateTimeline`, `AssumptionToggle`, `ArchiveGraph`, `BuildStatusPanel`.

Pending: all component code. Use interaction to deepen understanding, not as decoration.
