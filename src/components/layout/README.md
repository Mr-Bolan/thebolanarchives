# src/components/layout/

Global layout shell.

- `SiteShell` — wrapper: header, nav, main content area, footer, optional background texture.
- `Header` — shows `thebolanarchives` + optional status line `anonymous build records`.
- `Footer` — quiet footer (e.g. `nothing here is final. some of it is useful.`).

Pending: all component code. Keep the outer shell minimal; let the article body be rich.
