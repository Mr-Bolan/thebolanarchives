# src/components/navigation/

Site navigation.

- `Nav` — minimal, persistent links to the major sections (`/entries`, `/field-notes`,
  `/build-logs`, `/fragments`, `/patterns`, `/experiments`, `/graveyard`, `/index`, `/about`).

Pending: component code. Keep it sparse and lowercase.
