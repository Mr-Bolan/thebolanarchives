# public/textures/

Subtle background textures used by `SiteShell` (faint grid, paper grain, blueprint lines,
scanlines). Keep them very low contrast — never make reading harder. Reference as
`/textures/<file>`. Pending: assets.
