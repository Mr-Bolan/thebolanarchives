# publish content prompt

Start at `AGENTS.md`. Use mode `content-publish`.

Task:

```text
Prepare this archive record for publication.
Check frontmatter, anonymity, theme, status/confidence, related links, draft/unlisted/public behavior, and public metadata.
Do not redesign the site or change deployment.
Run `npm run content:audit`, `npm run privacy:audit`, `npm run build`, and `npm run pages:verify`.

Record or slug:
<paste slug/path here>

Target visibility:
<public | unlisted | draft>
```
