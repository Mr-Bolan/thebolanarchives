# update content prompt

Start at `AGENTS.md`. Use mode `content-update`.

Task:

```text
Update the existing archive record below.
Preserve slug, filename, record_id, created, and type unless explicitly told otherwise.
Keep status and confidence honest; do not polish uncertainty away.
Do not change routes, components, styles, deploy settings, or unrelated content.
Run the required checks from `docs/agent-workflow.md`.

Record or slug:
<paste slug/path here>

Requested change:
<paste change here>
```
