# site feature prompt

Start at `AGENTS.md`. Use mode `site-feature`.

Task:

```text
Add the smallest site feature that satisfies the request.
Preserve existing routes, deployment model, anonymity, and Blackbox Garden theme unless explicitly told otherwise.
Do not add CMS, database, search, or custom interactive modules unless the request names them.
Update docs only where behavior changes.
Run `npm run agent:check`.

Feature:
<paste request here>
```
