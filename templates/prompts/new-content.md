# new content prompt

Start at `AGENTS.md`. Use mode `content-capture` for rough notes or `content-draft` for a real MDX record.

Task:

```text
Add a new archive record from the notes below.
Keep the archive anonymous and follow `docs/archive-style-guide.md`.
Use `docs/content-model.md` and the matching `templates/content/*.mdx`.
Do not change routes, components, styles, deploy settings, package files, or unrelated content.
Run the required checks from `docs/agent-workflow.md`.

Notes:
<paste notes here>
```
