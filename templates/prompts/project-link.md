# project link prompt

Start at `AGENTS.md`. Use mode `project-link`.

Task:

```text
Add or update external project links for this archive record.
Use `external_links` exactly as documented in `docs/project-linking.md`.
Do not add a `/projects` route, CMS, database, search, or unrelated design changes.
Run the required checks from `docs/agent-workflow.md`.

Record or slug:
<paste slug/path here>

Links:
<paste label, url, kind here>
```
