# content/patterns/

Reusable mental models and frameworks (e.g. Layer 0-4 thinking, data trust levels,
dashboard truth vs appearance, operator knowledge as evidence).
Content type: `pattern`. Route: `/patterns`.

Maturity statuses common here: `sketch`, `working_note`, `field_tested`, `stable_artefact`.

Naming: `slug.mdx`. `README.md` is ignored by the loader.
