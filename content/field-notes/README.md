# content/field-notes/

Observations from real systems, machines, operators, work, data, and mistakes.
Content type: `field_note`. Route: `/field-notes`.

Short to medium. Honest about uncertainty — pair with a `confidence` value.

Maturity statuses common here: `sketch`, `working_note`, `field_tested`.

Naming: `slug.mdx`. `README.md` is ignored by the loader.
